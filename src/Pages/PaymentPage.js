import React from "react";

const PaymentPage = ({ paymentMethod }) => {
  return (
    <div>
      <h2>Payment Method: {paymentMethod}</h2>
      {/* Display the rest of the payment details and form for completing the payment */}
      {/* Add your payment page content here */}
    </div>
  );
};

export default PaymentPage;
